# 하이리온 로봇 설계 방법론

---

## 전체 흐름: 요구사항 → 제약 → 설계

모든 설계 결정은 이 순서로 정당화되어야 한다:

```
1. 요구사항 (Requirements)   "무엇을 해야 하는가"
        ↓
2. 제약조건 (Constraints)    "무엇이 한계인가"
        ↓
3. 설계 (Design)             "어떻게 만들 것인가"
        ↓
4. 검증 (Validation)         "설계가 요구사항을 만족하는가"
```

하이리온 적용 예시:
- **요구사항**: 쇼 30분, 팔로 컵 집기, 걷기
- **제약**: 총 무게 20kg 이하, 높이 1m, 배터리 교체 가능
- **설계**: 3층 선반 + 배터리 후면 슬롯 + BHL 다리 그대로

---

## 고려사항의 구분

크게 **필수(Must)** 와 **선택(Nice-to-have)** 로 나뉜다.

### 필수 고려사항

**① 하중 경로 (Load Path)**
- 힘이 어떤 경로로 땅까지 전달되는지
- 취약한 단일 지점(Single Point of Failure)이 없어야 함
- 하이리온에서 가장 중요한 곳: **토르소 브래킷** (상체 4.5kg의 모든 하중이 여기 집중)

**② 무게중심 (CoM)**
- 보행 로봇에서 가장 민감한 파라미터
- 설계 초기에 spreadsheet로 계산하고, 변경할 때마다 업데이트
- 하이리온은 BHL 보행 정책이 상체 없는 상태로 학습됨 → CoM 변화가 보행 안정성에 직결

**③ 인터페이스 정의**
- 두 부품이 만나는 지점을 먼저 결정
- "어떻게 붙이는가"를 먼저 결정하면 각 파트 설계가 독립적으로 진행 가능
- 예: BHL 상단 볼트 패턴 확인 → 토르소 브래킷 하단 홀 패턴 결정 → 그 다음 선반 설계

**④ 조립 순서**
- 나중에 넣을 수 없는 부품이 생기면 전체를 다시 분해해야 함
- 설계 전에 "어떤 순서로 조립하는가"를 시뮬해볼 것
- 하이리온: 배터리를 3층에 먼저 넣고 선반 올리는지, 선반 다 만들고 옆으로 밀어넣는지

**⑤ 유지보수 접근성**
- 자주 교체/점검하는 부품은 항상 외부에서 접근 가능해야 함
- 하이리온: 배터리(30분마다), 비상정지 스위치, USB 연결부

---

### 선택적 고려사항 (프로젝트 규모에 따라)

| 고려사항 | 이 프로젝트 적용 여부 |
|---|---|
| 안전율 (Safety Factor) 계산 | 브래킷 정도만 — 2.0 이상이면 충분 |
| 열 해석 | 정밀 계산보다 환기구 + 팬으로 경험적 해결 |
| 진동/피로 해석 | 쇼용 단기 운용이라 불필요 |
| 공차 스택업 분석 | 3D 프린트 파트는 ±0.3mm 여유 주면 충분 |
| IP 등급 (방수/방진) | 실내 쇼라 불필요 |

---

## 이 프로젝트에 맞는 실용적 설계 순서

```
Step 1. 인터페이스 확정 (Interface-First)
  → BHL 상단 볼트 패턴 실측
  → SO-ARM 베이스 볼트 패턴 확인
  → 이 두 개가 확정되면 몸통 설계 시작 가능

Step 2. 레이아웃 스케치 (2D 먼저)
  → 몸통 단면도: 가로×세로 안에 모든 부품이 들어가는가?
  → CoM 위치 계산 (무게 × 높이의 가중 평균)
  → CadQuery/Onshape로 넘어가기 전에 종이나 draw.io로 먼저

Step 3. 최소 기능 버전 (Minimum Viable Design)
  → 일단 토르소 브래킷 하나만 만들어서 BHL에 붙여보기
  → 전체 조립 후 문제 파악 → 수정
  → 이걸 반복하는 게 "빠른 설계"

Step 4. 검증
  → 물리: 브래킷에 4.5kg 매달아서 크랙 여부 확인
  → 열: Jetson + NUC 풀 부하로 10분 돌려서 온도 측정
  → 보행: URDF mass 업데이트 후 IsaacLab에서 시뮬
```

---

## 지금 당장 해야 할 설계 결정

`design_progress.md` 기준 **"BHL 치수 파악 중"** 단계에서 다음이 병목이다:

| 결정 | 왜 지금 해야 하나 |
|---|---|
| BHL 상단 마운트 볼트 패턴 | 이게 없으면 브래킷 설계 시작 불가 |
| 몸통 가로 폭 결정 | 2020 프로파일 4개의 간격 → BHL hip width(160mm) 기준으로 정해야 함 |
| 배터리 슬롯 탈착 방향 | 후면 슬라이드 vs 바닥 탈착 → 선반 구조가 달라짐 |

이 세 가지가 확정되면 나머지 설계가 대부분 자동으로 따라온다.